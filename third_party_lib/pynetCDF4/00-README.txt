This directory has scripts, etc that help build the netCDF4 python package, and its dependencies. The dependencies are the hard part!

See the top of build_deps.py for the details.

You should be able to run:

python build_deps.py

and get the whole thing. It build netcdf4 statically be default


